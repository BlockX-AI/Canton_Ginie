# frontend_light

This directory is reserved for a lightweight frontend implementation.

Currently unpopulated. The active frontend is `frontend_dark/`.

If you intend to build here, start from `frontend_dark/` as a reference.
